# Archived demo v1

This folder preserves the previous illustrated tokenized-deposit demo and its original dependencies:

- `index.html`
- `styles.css`
- `app.js`
- `demo-spec.json`

Open `index.html` directly to run the archived version. The current demo is [`demo/index.html`](../../index.html).
